# doc-api-swagger
How to document an Api - Example

It is an example to how to document an API using Swagger (https://swagger.io/).

The documentation can be built using online editor (http://editor.swagger.io/), for example, or usinf annotations.

This examples was built using annotations.

Access wiki to see more detail.
